# Plan 276 compatibility-shaped Axum consumer

Comparison fixture for the pre-extraction package profile: core's `tower`
feature plus the direct server and Axum. It runs the same wire/lifecycle suite
as the direct fixture. Core is expected to retain its static/PHF closure.

Run `cargo test --locked` from this directory.
